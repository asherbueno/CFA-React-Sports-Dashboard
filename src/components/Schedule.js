import React from 'react';
import { Card } from 'reactstrap';

const Schedule = (props) => {
  return(
    <Card>
      <div>
        <h2>Schedule</h2>

      </div>
    </Card>
  )
};

export default Schedule;
